import type { ExperienceEntry } from "@/types";

/**
 * Professional experience.
 *
 * Intentionally empty. The GitHub profile lists Quantum Bytes Solution, but role,
 * dates and responsibilities have not been supplied — see PROJECT_INFO.md. The
 * Experience section does not render while this array is empty, so nothing
 * unverified is published.
 */
export const experience: ExperienceEntry[] = [];
